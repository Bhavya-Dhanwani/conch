export default function NicheShowcase({ template }) {
  return (
    <section aria-label="Car Repair Garages showcase">
      <h2>{template.title}</h2>
      <p>{template.description}</p>
    </section>
  );
}
