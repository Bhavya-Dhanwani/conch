export default function NicheShowcase({ template }) {
  return (
    <section aria-label="Salons and Barbershops showcase">
      <h2>{template.title}</h2>
      <p>{template.description}</p>
    </section>
  );
}
