{
  "name": "conch",
  "private": true,
  "packageManager": "npm@11.10.1",
  "workspaces": [
    "application"
  ],
  "scripts": {
    "dev": "npm --prefix application run dev",
    "build": "npm --prefix application run build && node scripts/prepare-vercel-next-output.cjs",
    "start": "npm --prefix application run start",
    "lint": "npm --prefix application run lint"
  },
  "dependencies": {
    "settings": "^0.1.1"
  }
}
